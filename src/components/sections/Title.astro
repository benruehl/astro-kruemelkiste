---
import Flare from '../common/Flare.astro';
import TitleImage from '../common/TitleImage.astro';

const baseUrl = import.meta.env.BASE_URL;
const textBackgroundImageUrl = `url(${baseUrl}/title-bg.jpg)`.replace("//", "/");
---

<section>
    <div class="flares">
        <Flare color="yellow" width="1900" height="1234" x="10%" y="-5%"/>
        <Flare color="orange" width="700" height="700" x="40%" y="-35%"/>
        <Flare color="blue" width="1100" height="700" x="15%" y="15%"/>
    </div>
    <div class="text">
        <h1>Steffis<br>Krümelkiste</h1>
        <p class="subtitle">Liebevolle Kindertagespflege in Weißensee</p>
        <p>
            Ich bin Steffi Rühl. In meiner Einrichtung betreue ich Kinder im Alter
            von 1-3 Jahren. Insgesamt fünf Kinder finden bei mir Halt und Geborgenheit
            in einem geregelten und dennoch abwechslungsreichen Tagesablauf.
        </p>
        <button type="button">Kontakt</button>
    </div>
    <TitleImage/>
</section>

<style lang="scss" define:vars={{ textBackgroundImageUrl }}>
    section {
        display: flex;
        position: relative;
    }

    .subtitle {
        font-size: 1.4rem;
    }
    
	h1 {
        background-color: #6D7C48;
		background-image:
            linear-gradient(rgba(255,255,255,0.2), rgba(255,255,255,0.2)),
            var(--textBackgroundImageUrl);
		background-clip: text;
		-webkit-text-fill-color: transparent;
		background-size: cover;
	}

    p {
        background: linear-gradient(to right, rgb(var(--text-color)), rgb(var(--text-color-strong)));
        background-clip: text;
        -webkit-text-fill-color: transparent;
    }

    button {
        font-family: Dosis, "sans-serif";
        font-size: 1.2rem;
        font-weight: 600;
        background: linear-gradient(
            to right,
            rgba(var(--orange-strong), 0.6),
            rgba(var(--orange), 0.7)
        );
        border: none;
        color: white;
        padding: .6em 2em;
        border-radius: 2em;
        margin-top: 1em;
        cursor: pointer;
        position: relative;

        &::before {
            content: "";
            position: absolute;
            bottom: 0;
            left: 0;
            right: 0;
            height: 1em;
            background: rgb(var(--orange));
            z-index: -1;
            filter: blur(20px);
            opacity: 0;
            transition: opacity .3s ease;
        }

        &:hover {
            &::before {
                opacity: 1;
            }
        }
    }

    .flares {
        position: absolute;
        left: 0;
        right: 0;
        top: 0;
        bottom: 0;
        z-index: -1;
        display: grid;
        grid-template-areas: "overlay";
        align-items: center;
        justify-content: center;

        :global(*) {
            grid-area: overlay;
        }
    }
</style>
